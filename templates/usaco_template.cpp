/*
   user: {{_author_}}
   team: Los Enciclados
   created: {{_date_}}
*/

#include <bits/stdc++.h>

using namespace std;

using ll = long long;

const int INF = 1e9;
const double EPS = 1e-9;

int main() {
   ios::sync_with_stdio(0);
   cin.tie(0);

   #ifndef USACO_IO
      freopen("{{_variable_}}.in", "r", stdin);
      freopen("{{_variable_}}.out", "w", stdout);
   #endif
   
   {{_cursor_}}

   return 0;
}
